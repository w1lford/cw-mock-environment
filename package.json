{
  "name": "clean-starting-point",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc -p ./tsconfig.json",
    "start": "tsc -p ./tsconfig.json && npx http-server . -c-1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/arcgis-js-api": "^4.28.4",
    "arcgis-js-api": "^4.24.0"
  },
  "devDependencies": {
    "typescript": "3.7"
  }
}
